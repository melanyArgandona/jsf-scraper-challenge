/**
 * Fachada de red. Re-exporta la implementación principal para mantener la
 * convención de carpetas documentada (capa `http`) sin duplicar código.
 */
export { HttpClient } from "../services/HttpClient";